#ifndef MODE_H
#define MODE_H

// Definizione dell'enum per i modi di funzionamento del condizionatore
typedef enum {
    NONE = 0,
    HEATER = 1,
    COOLER = 2,
    HUMIDIFIER = 3,
    WIND = 4
} Mode;

#endif // MODE_H
